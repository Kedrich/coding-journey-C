#include <stdio.h>

/*
void copy_value(int *ptr1, int *ptr2)
{
    *ptr1 = *ptr2;
}

int main(void)
{

    int x, y;
    printf("Please enter 2 numbers: ");
    scanf("%d %d", &x, &y);

    copy_value(&x, &y);

    printf("%d %d", x, y);

    return 0;
}
*/

/*

    PROBLEM 5

void addValues(int *ptr1, int *ptr2)
{
    *ptr1 = *ptr1 + *ptr2;
}

int main(void)
{

    int x, y;
    printf("Please enter 2 numbers: ");
    scanf("%d %d", &x, &y);

    addValues(&x, &y);

    printf("x = %d\ny = %d", x, y);

    return 0;
}
*/